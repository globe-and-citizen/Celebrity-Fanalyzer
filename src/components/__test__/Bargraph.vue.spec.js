import { describe, expect, it } from 'vitest'
import { render } from '@testing-library/vue'

// import BarGraph from "../BarGraph.vue";
// TODO make the test
// TODO Fix echart issue
describe('Testing Bargraph',()=>{
  it('should be able to mount the Bargraph', async () => {
  })
})
